{
  "routes": [
    {
      "method": "GET",
      "path": "/facturas",
      "handler": "factura.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/facturas/count",
      "handler": "factura.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/facturas/:id",
      "handler": "factura.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/facturas",
      "handler": "factura.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/facturas/:id",
      "handler": "factura.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/facturas/:id",
      "handler": "factura.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
